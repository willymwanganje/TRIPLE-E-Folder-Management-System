import {createContext,useContext,useState} from 'react';const C=createContext(null);export function ToastProvider({children}){const[toasts,setToasts]=useState([]);const push=(message,type='success')=>{const id=Date.now();setToasts(t=>[...t,{id,message,type}]);setTimeout(()=>setToasts(t=>t.filter(x=>x.id!==id)),3000)};return <C.Provider value={{push}}>{children}<div style={{position:'fixed',right:20,bottom:20,zIndex:9999,display:'grid',gap:10}}>{toasts.map(t=><div key={t.id} style={{background:t.type==='error'?'#991b1b':'#065f46',color:'#fff',padding:'12px 16px',borderRadius:10,boxShadow:'0 8px 30px #0002'}}>{t.message}</div>)}</div></C.Provider>}export const useToast=()=>useContext(C);
