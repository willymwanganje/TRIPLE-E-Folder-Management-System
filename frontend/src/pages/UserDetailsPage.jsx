import {useEffect,useState} from 'react';import {useParams} from 'react-router-dom';import {api} from '../services/api';export default function UserDetailsPage(){const{id}=useParams();const[u,setU]=useState(null);useEffect(()=>api.user(id).then(setU),[id]);if(!u)return <div>Loading...</div>;return <div style={{background:'#fff',padding:25,borderRadius:14}}><h1>{u.fullName}</h1><p>{u.email}</p><p>{u.role?.name}</p><p>{u.isActive?'Active':'Inactive'}</p></div>}
